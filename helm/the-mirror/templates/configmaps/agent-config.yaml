{{- if .Values.agent.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: mirror-agent-config
  namespace: {{ include "the-mirror.namespace" . }}
  labels:
    {{- include "the-mirror.labels" . | nindent 4 }}
    app: mirror-agent
data:
  action-pool.yaml: |
{{ .Files.Get "config/action-pool.yaml" | nindent 4 }}
  suspicious-user-agents.yaml: |
{{ .Files.Get "config/suspicious-user-agents.yaml" | nindent 4 }}
{{- end }}
